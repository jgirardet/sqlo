A faire:

- permettre les alias dans where et autres avec pg: il faudrait mettre le colexpr du cast dans les aliass réécrir le content. ou est ce une mauvaise idée :
  - soit aggregate et donc c having.
  - soit c un fonction (genre repplace) et plutôt subquery ?
  - en tous cas ligne 429: alias with call qui serait mieux remplacé
  - ou on garde la tokenstream et on la réinject pour la réinterpréter ?
- force type with pg argument
  \_ add ! and ? for joins

# Bug:

- rediscuter le typeoverride au bout du compte ?
- delete with foreign key (delete maison[1])
- Serializer de create fn et fk doit retourner null et non empty string

# à faire peut être :

- to_json method
- from_json

## Faire pour tuer le temps:

-test sqlo\*set de query

- test Vec<u8>

# Passage à pgsql

- any, all

# plus tard

passer à syn 2
