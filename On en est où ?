A faire:

 
CHANGE update macro to mkae it more consitente
update_MyStruct![instance; field1=value1, field2=value2](&pool).await?
update_Mystruct![pk = value, field1=value1, field2=value2](&pool).await?
- update![MyStruct[pk] field1=::value1 where]
- update![MyStruct(instance) field1=::value1 where]

# Bug:

- histoire de drop avec clippy et update ??
- Serializer de create fn et fk doit retourner null et non empty string

# à faire peut être :

- a'& str non supporté : il faut supporter les lifetime de la struct pout cela.
  ensuite supporter a'str dans types
- to_json method

## Faire pour tuer le temps:

-test sqlo\*set de query

- test Vec<u8>

# Passage à pgsql

- any, all
